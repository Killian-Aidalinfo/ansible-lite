GLOBAL:
  type: client # Client / Server or both
  db_path: data/db.sqlite3
  log_path: data/log.txt
  log_level: info
  repos_config: repos.yaml
  port: 8082 # Used only for server
  credentials: 
  # ssl: false 
  # cert: data/cert.pem
  # key: data/key.pem